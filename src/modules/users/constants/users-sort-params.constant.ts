export enum UsersSortBy {
  Email = 'email',
  FirstName = 'firstName',
  LastName = 'lastName',
  CreatedAt = 'createdAt',
  UpdatedAt = 'updatedAt',
}

export enum UsersSortOrder {
  Asc = 'ASC',
  Desc = 'DESC',
}
